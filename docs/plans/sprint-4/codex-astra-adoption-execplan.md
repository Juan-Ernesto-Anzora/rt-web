# Adopt GPT-6 Astra for the Codex development workflow

## Purpose

Adopt GPT-6 Astra as the checked-in Codex development default for `rt-web` and make the repository's instructions, prompts, paths, commands, review gates, and handoffs accurate for the completed Sprint 3 baseline. Sprint 4 is an organizational label for this workflow change. It does not authorize Request Tracker product features, API changes, SQL work, dependency installation, deployment, or secret handling.

The observable result is a small documentation/configuration diff that a new Codex task can follow without reopening completed Sprint 2/3 work, inventing unavailable runners, weakening quality gates, or pausing on routine reversible choices. The active local Codex config already selects Astra and retains a supported reasoning effort, so no home-directory change is required.

## Repository orientation

This plan applies to `C:/dev/rt_project/rt-web-skeleton` on branch `docs/codex-astra-adoption`. It was created from commit `ada3bb3`, then fast-forwarded to current `main` at merge commit `574f910` after Web final-hardening PR #21 merged. The adoption PR targets `main`.

Relevant files:

- `AGENTS.md`: repository-wide architecture, paths, quality gates, Git rules, and agent workflow.
- `.agent/PLANS.md`: required ExecPlan format and maintenance rules.
- `.agent/code_review.md`: read-only review priorities and output contract.
- `docs/codex-prompts.md`: reusable context, implementation, review, and handoff prompts.
- `.codex/config.toml.example`: checked-in Codex default example; it must select `gpt-6-astra` without adding credentials or overriding managed controls.
- `docs/plans/sprint-3/web-admin-configuration-execplan.md`: completed implementation history and remaining real-browser/deployment evidence.
- `docs/sprint-3-web-verification.md`, `docs/sprint-3-known-issues.md`, and `docs/sprint-3-demo-script.md`: current verification modes, accepted limitations, and blocked live browser evidence.
- `package.json`, `playwright.config.ts`, `eslint.config.js`, `vite.config.ts`, `tsconfig.json`, `pnpm-lock.yaml`, and `.github/workflows/ci-web.yml`: actual Web runners and CI gates.
- `README.md`, `CODEOWNERS`, `design/design-tokens.json`, and `.env.example`: current source layout and onboarding paths.
- Sibling API contract/evidence: `C:/dev/rt_project/rt-api-skeleton/docs/sprint-3-api-verification.md`, generated `GET /api/schema`, and API final-hardening PR #22.

No nested `AGENTS.md` exists under this Web checkout, so the root file applies throughout.

## Current behavior

The repository's active local Codex config at `C:/Users/Juan Anzora/.codex/config.toml` already contains `model = "gpt-6-astra"` and `model_reasoning_effort = "medium"`. Official OpenAI documentation identifies `gpt-6-astra` as the target model and supports `medium` reasoning. The local config also contains MCP, project trust, plugin, shell, Windows sandbox, and desktop settings; this task must not rewrite or expose those values. The active model selected for this running conversation is not available through a client-visible status surface, so it remains unverified.

The Request Tracker application contains no OpenAI SDK, Responses API call, model router, agent runtime, or API key configuration. `.agent`, `.codex`, `AGENTS.md`, and `docs/codex-prompts.md` control Codex development behavior only. Any future application-level OpenAI integration is a separate migration surface.

Sprint 3 implementation is complete and Web final-hardening PR #21 is merged into `main` at `574f910`. API final-hardening PR #22 is also merged and API release evidence reports SQL/Postman/MailHog verification and disposable cleanup. Neither Git history nor these docs prove current deployment state. The real authenticated 25-step Web browser demo remains explicitly unverified.

Current guidance contains drift:

- `AGENTS.md` points to `/design-tokens.json`; the file is `design/design-tokens.json`.
- It names a static `openapi-rt-with-examples.yaml`; the current contract is generated by the sibling API at `GET /api/schema`.
- It advertises `docker compose up` and `scripts/seed.sh`, neither of which exists in this Web checkout.
- It names shadcn/ui, but the package uses local components and has no shadcn/Radix dependency.
- It lists frontend test tooling generically; actual runners are TypeScript, ESLint, Vite build, and Playwright Chromium. Vitest/React Testing Library are not installed.
- `docs/codex-prompts.md` still opens with a Sprint 2 Day 3-4 prompt and an ambiguous "next unchecked milestone" prompt.
- README still labels the project a scaffold and uses POSIX-only `cp` in a Windows workspace.
- CI watches `.eslintrc*`, while the actual file is `eslint.config.js`.
- `CODEOWNERS` has `/web/**`; application source is under `/src/**`.
- Historical Sprint 3 plan entries still say settings/polish work was uncommitted or lacked Playwright. Those entries are history, not current work.

## Desired behavior

- The checked-in example selects `gpt-6-astra` and changes no API/runtime model.
- This task does not edit the local active config because its model and `medium` effort are already supported; MCP, auth, permissions, trust, plugins, sandbox, service tier, and shell settings remain outside this repository change.
- Instruction precedence is explicit: runtime-managed controls and explicit user scope are preserved; root/nested repository instructions and the selected active ExecPlan guide repository work. Historical plans are evidence, not automatic authorization.
- Codex completes authorized work end to end, makes routine reversible choices that fit established repository patterns, and asks only when a material ambiguity changes the outcome or a real permission/managed-control boundary blocks progress.
- If an instruction causes a pause, block, or scope change, Codex names the file and applicable rule and distinguishes the rule from its interpretation.
- Reusable prompts require an explicit `{active_execplan}` path and never infer an active milestone from old unchecked boxes.
- Focused checks run while iterating. The complete required check set runs once at the PR gate and is rerun only after relevant changes, failures, or unresolved risk justify it.
- Missing tools remain documented requirements or explicit gaps; prompts never claim they ran or silently replace them with invented commands.
- Sprint 3 code completion, PR state, mocked test evidence, API evidence, and uncertain deployment/live-browser state remain distinct.

## Scope

In scope:

- Add this ExecPlan.
- Update `AGENTS.md`, `.agent/PLANS.md`, `.agent/code_review.md`, `docs/codex-prompts.md`, `.codex/config.toml.example`, `README.md`, `CODEOWNERS`, `.github/workflows/ci-web.yml`, `playwright.config.ts`, and Sprint 3 plan/verification/known-issue handoffs only as needed to reconcile workflow guidance, current evidence, and the confirmed CI smoke defect.
- Validate internal paths, links, TOML syntax, YAML readability, and current Web quality gates.
- Prepare a concise PR title/body.

Out of scope:

- Changes under `src/`, application OpenAI integration, model API calls, prompts shipped to end users, SQL, migrations, dependencies, package versions, deployment, live credentials, MCP configuration, permissions, or auth state.
- Editing `C:/Users/Juan Anzora/.codex/config.toml`; it already has the target model and supported effort.
- Marking the real Sprint 3 browser demo or any deployment as passed without new evidence.
- Restarting completed Sprint 2/3 implementation from historical unchecked boxes.

## Implementation plan

### Milestone 1: Reconcile instruction sources

Update `AGENTS.md` to point to actual token/API/setup paths and runners. Add concise instruction-hierarchy, autonomous follow-through, blocking transparency, selected-plan, and focused/full-check guidance. Keep security, tenant isolation, Git, review, and full PR gates intact.

Update `.agent/PLANS.md` so an active plan is selected explicitly, completed/historical plans do not become active from unchecked boxes alone, focused iteration checks are distinct from full PR checks, and blockers name their instruction source.

Update `.agent/code_review.md` to require the selected plan and actual Web checks, preserve read-only review behavior, and distinguish mocked browser evidence from live-service evidence.

### Milestone 2: Replace prompt entry points and set Astra example

Replace `docs/codex-prompts.md` with Context, Implement, Review, and Handoff templates that require `{active_execplan}`. Include scope completion, routine reversible choices, material ambiguity, managed permission boundaries, instruction-caused blocking, check calibration, evidence modes, and no historical-plan restart.

Add `model = "gpt-6-astra"` to `.codex/config.toml.example`. Do not add an API key or duplicate the home config. Record that the home config already uses Astra with `medium` reasoning and was not modified.

### Milestone 3: Reconcile onboarding, ownership, and CI paths

Update README with current Web purpose, Windows/POSIX environment-copy commands, actual scripts, mocked Playwright behavior, and sibling API contract/live-service pointers.

Change `CODEOWNERS` `/web/**` to `/src/**` while retaining the existing owner. Update CI path filters to include `eslint.config.*` and the Codex workflow files whose PRs must exercise the full quality gate.

### Milestone 4: Validate and prepare review

Validate every referenced repository path with `Test-Path`/`rg --files`. Parse `.codex/config.toml.example` with Python 3.11+ `tomllib` or another already-installed TOML parser; do not install one. Parse CI YAML only with an already-installed parser, otherwise inspect it and report the missing parser rather than inventing a runner.

Run focused documentation checks during editing. At the PR gate run `pnpm typecheck`, `pnpm lint`, `pnpm build`, `pnpm test`, and `git diff --check`; use documented npm equivalents only if the Codex Windows pnpm wrapper fails before script execution, and report the distinction. No live API/SQL services are required because no product code or contract changes.

Review `git diff --check`, changed-file scope, links, no-secret patterns, and the stacked-branch base. Prepare a draft PR description; do not push or create a PR unless separately requested.

### Milestone 5: Reconcile merged main and repair CI smoke setup

Fetch and inspect current `origin/main` plus the attached failed `ci-web / build` log. Fast-forward the adoption branch after PR #21 merges. Reproduce the CI environment by ensuring the Playwright-managed Vite server has an explicit test API base, while retaining contract-shaped browser interception and no live-service dependency. Run all required Web checks, update this plan with the evidence, then commit, push, and create the requested PR against `main`.

## Tests and verification

Focused documentation validation:

- `rg --files` and `Test-Path` for every referenced local file/directory.
- TOML parse of `.codex/config.toml.example`.
- Confirm the active home config's model/effort fields only; do not display other values.
- `git diff --check` after each material documentation group.

Required PR gate:

```powershell
pnpm typecheck
pnpm lint
pnpm build
pnpm test
git diff --check
```

Documented Windows fallback when the pnpm wrapper aborts before running scripts:

```powershell
npm.cmd run typecheck
npm.cmd run lint
npm.cmd run build
npm.cmd test
git diff --check
```

`pnpm test`/`npm.cmd test` runs Playwright with intercepted contract-shaped APIs; it is not live API/SQL/Postman/MailHog evidence.

## Acceptance criteria

- Only workflow/config/docs/CI/test-runner ownership files and this plan change; `src/`, SQL, dependencies, and lockfiles are untouched.
- `.codex/config.toml.example` parses and contains exactly `model = "gpt-6-astra"` as the model selection.
- Active home config remains unchanged, retains `model_reasoning_effort = "medium"`, and preserves all non-model settings.
- `AGENTS.md` references `design/design-tokens.json`, sibling API generated `/api/schema`, and actual Web setup/check commands.
- Instructions preserve required full gates and clearly distinguish focused iteration checks, mocked Playwright, and live-service verification.
- Prompts name `{active_execplan}`, cover context/implementation/review/handoff, and do not default to Sprint 2 or an inferred unchecked milestone.
- Instructions require authorized follow-through, routine reversible decisions, material-ambiguity/permission-only questions, and source-named instruction blockers.
- README, CODEOWNERS, and CI paths match the checkout.
- Sprint 3 is recorded as the completed code baseline and Web PR #21 as merged; real deployment/browser evidence remains accurately unverified until confirmed.
- No OpenAI application dependency, API call, API key, credential, MCP/auth/permission change, product feature, or SQL change is introduced.
- Required checks pass or their exact environment/tooling blocker is recorded without claiming success.
- The diff is small, reviewable, and has a prepared draft PR description.

## Progress

- [x] Official OpenAI GPT-6 Astra guidance searched and read.
- [x] Repository/sibling handoff, instruction hierarchy, active config model/effort, runners, CI, and Sprint 3 evidence inspected.
- [x] Short-lived `docs/codex-astra-adoption` branch created from Web final-hardening commit `ada3bb3`.
- [x] Sprint 4 adoption ExecPlan created.
- [x] Instruction and plan standards reconciled.
- [x] Prompt and checked-in Codex example updated.
- [x] README, ownership, and CI paths reconciled.
- [x] Documentation/TOML/YAML references and required PR checks validated.
- [x] PR description prepared and outcome recorded.
- [x] Current `main` and the attached failed merge-run log inspected; the branch fast-forwarded to merged PR #21.
- [x] Confirmed CI Playwright API-base and audit-fixture defects fixed; full PR gate rerun.
- [x] Adoption changes prepared for the requested commit, push, and PR against `main`; publication evidence is reported with the resulting PR URL.

## Surprises & Discoveries

- 2026-09-12: The active home Codex config already selects `gpt-6-astra` with `medium` reasoning. No local merge is needed, and editing it would create needless risk to retained MCP/auth/trust/plugin settings.
- 2026-09-12: No active model status for this running task is exposed through a client-visible surface available to this task. The home default is evidence about configuration, not proof of the current conversation model.
- 2026-09-12: The Request Tracker Web package has no OpenAI SDK or runtime agent integration. This adoption is development workflow configuration only.
- 2026-09-12: Web final-hardening PR #21 remains open as a draft, so the adoption branch is intentionally stacked on `ada3bb3` rather than branched from an older `main`.
- 2026-09-12: API final-hardening PR #22 is merged, but deployment is not established by Git/PR status.
- 2026-09-12: The repository has Playwright but no Vitest/React Testing Library. Existing generic test wording can otherwise lead an agent to invent or claim missing runners.
- 2026-09-12: The official guide warns that Astra is particularly sensitive to instruction files such as `AGENTS.md`, making stale paths and historical prompts a migration risk rather than cosmetic documentation debt.
- 2026-09-12: The Codex Windows `pnpm` wrapper aborted before each script with `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY`. The documented `npm.cmd` equivalents executed the same package scripts successfully.
- 2026-09-12: Documentation validation resolved 24 referenced repository paths with no missing path; the checked-in Codex example parsed as TOML with `model = "gpt-6-astra"`; and the Web CI workflow parsed as YAML.
- 2026-09-12: The production build completed with 139 transformed modules and only the existing Browserslist data-age warning. Seven mocked Chromium Playwright smoke tests passed in 19.6 seconds.
- 2026-09-12: The home config file fingerprint changed externally during this task. This task did not write that file; a final non-secret field check still showed `gpt-6-astra` with `medium` reasoning. No inference is made about the model running this conversation.
- 2026-09-14: Web PR #21 is merged into `main` at `574f910`. Its merge-run installed dependencies and passed TypeScript, ESLint, and Vite build, but Playwright reported six failures and one pass.
- 2026-09-14: All six failures stopped in the shared login helper waiting for the Home heading. The merged mock refactor narrowed interception to `**/api/**`, while the CI Vite server had no `VITE_API_BASE`; login therefore posted to relative `/auth/jwt/create` outside the mock. This is a deterministic test-environment configuration defect, not six independent application failures.
- 2026-09-14: After adding the explicit Playwright API base, six tests passed and the remaining Admin navigation test exposed a second mock-refactor regression: the audit fixture used unsupported `object_type`, `action`, and `timestamp` fields instead of the API's `type`, `entity_type`, and `created_at` contract. Restoring the contract-shaped fixture produced seven passing CI-mode tests.

## Decision Log

- 2026-09-12: Preserve the explicit target `gpt-6-astra`; do not run a latest-model resolver or substitute another model.
- 2026-09-12: Preserve active `medium` reasoning because it is supported; do not add a reasoning override to the repository example.
- 2026-09-12: Do not edit the active home config because it already matches the target. The checked-in example is the only model configuration edit.
- 2026-09-12: Keep the change stacked on Web final-hardening until PR #21 merges; then retarget/rebase before the adoption PR merges.
- 2026-09-12: Treat Sprint 3 implementation as complete code evidence. Retain the unverified real browser/deployment items as evidence gaps, not implementation milestones to restart.
- 2026-09-12: Keep application-level OpenAI migration out of scope because no integration exists.
- 2026-09-12: Preserve all existing quality requirements while naming actual runners and missing tooling accurately.
- 2026-09-12: Report the four failed `pnpm` launcher attempts separately from the four passing `npm.cmd` package-script runs; do not describe the pnpm commands themselves as passed.
- 2026-09-12: Prepare the adoption PR against `chore/sprint3-final-hardening` while Web PR #21 is open, then retarget or rebase it onto `main` after that prerequisite merges.
- 2026-09-14: Supersede the temporary stacked-base decision now that PR #21 is merged. Fast-forward to `origin/main` and target the adoption PR directly at `main`.
- 2026-09-14: Set `VITE_API_BASE=http://127.0.0.1:8000/api` only in Playwright's managed Web-server environment. This keeps CI self-contained, makes every shared-client request match the API-only route interception, and does not alter production defaults or require a live API.

## Outcomes & Retrospective

The bounded workflow adoption and confirmed merge-run repair are complete. The change covers repository instructions, prompts, Codex example configuration, onboarding/ownership/CI/test-runner configuration, a contract-shaped mocked audit fixture, and planning/verification evidence. There are no application source, API contract, SQL, dependency, lockfile, credential, or product-feature changes.

The checked-in example now selects `gpt-6-astra`. The active home config already selected `gpt-6-astra` with supported `medium` reasoning, so it was not edited; its MCP, authentication, trust, plugin, permission, and sandbox settings remain intact. No application-level OpenAI integration was found, so there is no separate runtime migration to perform from this checkout.

Validation outcomes:

- Repository references: 24 validated, 0 missing.
- `.codex/config.toml.example`: valid TOML; model is `gpt-6-astra`.
- `.github/workflows/ci-web.yml`: valid YAML; `build` job present.
- `pnpm typecheck`, `pnpm lint`, `pnpm build`, and `pnpm test`: launcher blocked before script execution by `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY`.
- `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build`: passed; the build transformed 139 modules.
- `CI=true npm.cmd test`: passed, seven of seven mocked Chromium smoke tests in 14.4 seconds with one worker and a fresh managed server. This is not live API, SQL, email, or deployed-environment evidence.
- `git diff --check`: passed after the final outcome update.

Sprint 3 remains the reported completed code baseline. Web PR #21 and API PR #22 are merged. Neither production deployment nor the full authenticated 25-step browser demo can be established from this checkout. Existing Prettier and Vitest/React Testing Library requirements remain without configured runners; this plan documents that gap rather than lowering or inventing a gate. The existing Browserslist data-age warning is non-blocking but remains visible.

Prepared PR title: `docs(codex): adopt GPT-6 Astra workflow`

Prepared PR base: `main`.

Prepared PR body:

    ## Summary
    - select GPT-6 Astra in the checked-in Codex example and reconcile instruction hierarchy, paths, commands, and contracts
    - replace stale Sprint 2 prompts with active-plan context, implementation, review, and handoff prompts
    - add the Sprint 4 adoption ExecPlan and reconcile Sprint 3 completion versus unverified deployment evidence
    - correct Web onboarding, CODEOWNERS coverage, and CI path triggers
    - fix the merged CI smoke setup by defining the Playwright API base and restoring an API-shaped audit fixture

    ## Validation
    - repository references: 24 valid, 0 missing
    - TOML and CI YAML parse successfully
    - npm.cmd run typecheck, lint, build, and test pass
    - CI-mode Playwright passes all seven Chromium smoke tests with a fresh server
    - pnpm launcher attempts are blocked before script execution by the documented Windows no-TTY issue
    - git diff --check passes

    ## Scope
    - no product code, API contract, SQL, dependency, lockfile, API key, or active local Codex config changes
    - Playwright uses mocked contract-shaped APIs; deployment and the full authenticated browser demo remain unverified

Next prompt:

    Read AGENTS.md, .agent/PLANS.md, .agent/code_review.md, and docs/plans/sprint-4/codex-astra-adoption-execplan.md in full. Review docs/codex-astra-adoption against chore/sprint3-final-hardening. Keep the review read-only. Verify instruction hierarchy, actual paths and runners, GPT-6 Astra configuration, Sprint 3 evidence reconciliation, and that no product or application-level OpenAI API changes were introduced. Return prioritized findings, then residual blockers and PR readiness.
