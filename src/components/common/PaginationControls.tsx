export function PaginationControls({ page, pageSize, count, label, onPageChange }: { page: number; pageSize: number; count: number; label: string; onPageChange(page: number): void }) {
  const totalPages = Math.max(1, Math.ceil(count / pageSize));
  return <nav aria-label={`${label} pagination`} className="flex flex-wrap items-center justify-between gap-3 text-sm text-neutral-700"><span aria-live="polite">Page {page} of {totalPages} · {count} {label}</span><div className="flex gap-2"><button type="button" disabled={page <= 1} onClick={() => onPageChange(page - 1)} aria-label={`Previous ${label} page`} className="rounded-lg border border-neutral-300 px-3 py-2 font-semibold hover:bg-white focus-visible:outline focus-visible:outline-2 focus-visible:outline-primary-600 disabled:opacity-50">Previous</button><button type="button" disabled={page >= totalPages} onClick={() => onPageChange(page + 1)} aria-label={`Next ${label} page`} className="rounded-lg border border-neutral-300 px-3 py-2 font-semibold hover:bg-white focus-visible:outline focus-visible:outline-2 focus-visible:outline-primary-600 disabled:opacity-50">Next</button></div></nav>;
}
